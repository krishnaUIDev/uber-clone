import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ProfileNav = () => {
  return (
    <View style={{ flex: 1, backgroundColor: "red" }}>
      <Text> ProfileNavProfileNav</Text>
    </View>
  );
};

export default ProfileNav;

const styles = StyleSheet.create({});
